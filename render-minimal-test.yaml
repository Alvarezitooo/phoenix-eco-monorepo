services:
- type: web
  name: phoenix-letters-test
  env: docker
  dockerfilePath: ./Dockerfile
  dockerBuildArgs:
  - APP_NAME=phoenix-letters
  plan: free
  envVars:
  - key: TEST_VAR
    value: test
