# 🚀 Phoenix Ecosystem - Monorepo Requirements
# Solution Gemini Pro Oracle avec fallback robuste

# Stratégie 1: Dependencies directes (fallback si package échoue)
streamlit
google-generativeai
tenacity
python-dotenv
protobuf
PyJWT
requests
bcrypt
supabase
plotly>=5.0.0
pandas>=1.5.0
cryptography>=3.4.8

# Stratégie 2: Package installable (idéal)
# -e ./apps/phoenix-letters